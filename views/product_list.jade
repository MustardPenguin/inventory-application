extends layout 

block content 
  div 
    a(href='/inventory/product/create') Create product
    p List of products:
    ul
      each product in products 
        li 
          div 
            a(href=product.url) #{product.name}
            |  ($#{product.price})
            div Seller: 
              a(href=product.seller.url) #{product.seller.name}
              | , Quantity: #{product.quantity}x